.easyclaw/
.DS_Store
__pycache__/
*.pyc
.venv/
node_modules/
.env
state/
!state/.gitkeep
# Per-project runtime state (2026-05-11 refactor)
projects/*/state/
# docs/demo_snapshot*/ is intentionally NOT ignored — it's the evidence trail
# for the GitHub Pages static demo. Root-level demo_snapshot*/ was a duplicate
# of the docs/ copies and got deleted on 2026-05-11.

genres/*/.build/

# AGENTS.md

> **注意**：本仓库只有一份权威的 `AGENTS.md`——根目录的 [../AGENTS.md](../AGENTS.md)。
>
> `docs/` 目录之前也放过一份同名文件（基于 2026-05-10 前的单层 `settings/<name>/` 结构），已随代码的 genre + project 两层重构过时。为避免双源漂移，本文件只留一个指针。
>
> 最新的目录页、state 地图、Agent 名册请看根目录那份。
